---
import { Play, Copy } from "lucide-react";
---

<section class="py-24 bg-background border-t border-white/5">
    <div class="max-w-7xl mx-auto px-4 md:px-8">
        <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
            <div>
                <div
                    class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-secondary/10 border border-secondary/20 text-secondary text-xs font-mono mb-6"
                >
                    <Play className="w-3 h-3 fill-current" />
                    Quick Start
                </div>
                <h2
                    class="text-3xl md:text-4xl font-bold font-heading text-white mb-6"
                >
                    Up and running in seconds.
                </h2>
                <p class="text-text-secondary mb-10 text-lg">
                    Newgate assumes sane defaults so you can skip the
                    boilerplate config and focus on your logic.
                </p>

                <div class="space-y-8">
                    <div class="flex gap-4">
                        <div
                            class="w-8 h-8 rounded-full bg-white/5 border border-white/10 flex items-center justify-center font-mono text-sm text-white shrink-0"
                        >
                            1
                        </div>
                        <div>
                            <h3 class="text-white font-bold mb-1">
                                Install the package
                            </h3>
                            <div
                                class="flex items-center gap-2 p-2 rounded bg-surface border border-surface-highlight w-fit"
                            >
                                <code
                                    class="text-sm font-mono text-primary px-2"
                                    >npm install newgatejs</code
                                >
                            </div>
                        </div>
                    </div>
                    <div class="flex gap-4">
                        <div
                            class="w-8 h-8 rounded-full bg-white/5 border border-white/10 flex items-center justify-center font-mono text-sm text-white shrink-0"
                        >
                            2
                        </div>
                        <div>
                            <h3 class="text-white font-bold mb-1">
                                Create your server
                            </h3>
                            <p class="text-text-secondary text-sm">
                                Create a default <code class="text-white/80"
                                    >server.js</code
                                > file.
                            </p>
                        </div>
                    </div>
                    <div class="flex gap-4">
                        <div
                            class="w-8 h-8 rounded-full bg-white/5 border border-white/10 flex items-center justify-center font-mono text-sm text-white shrink-0"
                        >
                            3
                        </div>
                        <div>
                            <h3 class="text-white font-bold mb-1">Run it</h3>
                            <div
                                class="flex items-center gap-2 p-2 rounded bg-surface border border-surface-highlight w-fit"
                            >
                                <code
                                    class="text-sm font-mono text-primary px-2"
                                    >node server.js</code
                                >
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="relative group">
                <div
                    class="absolute -inset-1 bg-gradient-to-r from-primary to-secondary rounded-xl blur opacity-20 group-hover:opacity-40 transition duration-1000"
                >
                </div>
                <div
                    class="relative rounded-xl bg-[#050505] border border-white/10 p-6 md:p-8"
                >
                    <div
                        class="absolute right-4 top-4 text-text-tertiary text-xs font-mono"
                    >
                        server.js
                    </div>
                    <pre
                        is:raw
                        class="font-mono text-sm md:text-base leading-relaxed overflow-x-auto"><code class="text-gray-300"><span class="text-primary">import</span> Newgate <span class="text-primary">from</span> <span class="text-secondary">'newgatejs'</span>;

<span class="text-text-secondary">// Initialize app</span>
<span class="text-primary">const</span> app = <span class="text-info">new</span> Newgate();

<span class="text-text-secondary">// Define a route</span>
app.<span class="text-info">get</span>(<span class="text-secondary">'/'</span>, (req, res) => {
  <span class="text-primary">return</span> res.json({ 
    hello: <span class="text-secondary">'world'</span>, 
    time: <span class="text-success">Date</span>.now() 
  });
});

<span class="text-text-secondary">// Start listening</span>
app.listen(<span class="text-secondary">3000</span>, () => {
  console.log(<span class="text-secondary">'Server running on port 3000'</span>);
});</code></pre>
                </div>
            </div>
        </div>
    </div>
</section>
